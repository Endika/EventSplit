import type { HistoryEntry } from '@/domain/entities/Event'

export function DiffViewer({ entry: _entry, onClose: _onClose }: { entry: HistoryEntry; onClose: () => void }) {
  return null // real implementation in B2
}
